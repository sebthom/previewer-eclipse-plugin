<?xml version="1.0" encoding="UTF-8"?>
<!--
   SPDX-FileCopyrightText: © Sebastian Thomschke and contributors.
   SPDX-FileContributor: Sebastian Thomschke
   SPDX-License-Identifier: EPL-2.0
   SPDX-ArtifactOfProjectHomePage: https://github.com/sebthom/previewer-eclipse-plugin
-->
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

   <modelVersion>4.0.0</modelVersion>

   <parent>
      <groupId>de.sebthom.eclipse.previewer</groupId>
      <artifactId>de.sebthom.eclipse.previewer.parent</artifactId>
      <version>1.0.0-SNAPSHOT</version>
   </parent>

   <artifactId>de.sebthom.eclipse.previewer.pikchr</artifactId>
   <packaging>eclipse-plugin</packaging>

   <properties>
      <!--
      [WARNING] Rule 0: org.apache.maven.plugins.enforcer.BanCircularDependencies failed with message:
      Circular Dependency found. Your project's groupId:artifactId combination must not exist in the list of direct or transitive dependencies.
        de.sebthom.eclipse.previewer:de.sebthom.eclipse.previewer.pikchr
       -->
      <skip.enforcer.basic-checks>true</skip.enforcer.basic-checks>
   </properties>

   <dependencies>
      <dependency>
         <groupId>org.junit.jupiter</groupId>
         <artifactId>junit-jupiter</artifactId>
         <version>6.1.3</version>
         <scope>test</scope>
      </dependency>
   </dependencies>

   <build>
      <plugins>
         <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <!-- Tycho compiles test sources for eclipse-plugin modules but does not bind the plain JVM test runner. -->
            <executions>
               <execution>
                  <id>execute-unit-tests</id>
                  <goals>
                     <goal>test</goal>
                  </goals>
               </execution>
            </executions>
            <!-- Do not allow this module to return to silently compiling without executing its tests. -->
            <configuration>
               <failIfNoTests>true</failIfNoTests>
            </configuration>
         </plugin>
      </plugins>
   </build>
</project>
